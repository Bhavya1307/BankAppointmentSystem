// api for adding employee
const addEmp = async (req,res) => {
    try {
        const { name, email, passsword, speciality, position, degree, experience, about, address } = req.body
    } catch (error) {
        
    }
}

export {addEmp}